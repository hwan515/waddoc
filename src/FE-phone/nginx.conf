# FE-phone (환자용 모바일) 컨테이너 내부 nginx 설정
# SPA 라우팅: 모든 경로를 index.html로 fallback
server {
    listen 3001;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
